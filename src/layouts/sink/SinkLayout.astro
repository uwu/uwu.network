---
import "^css/global.sass";
import BorderLayout from "@layouts/BorderLayout.astro";
import getProps from "@res/js/getProps";

const { title, bodyClass } = getProps(Astro.props);
---

<BorderLayout {...{ title, bodyClass }} titleName="sink" headerName="YELLOWSINK" mainHref="/~sink">
  <slot />
</BorderLayout>